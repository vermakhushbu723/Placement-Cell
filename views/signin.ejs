<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        
        .form-container {
            width: 450px;
            margin: 50px auto;
            text-align: center;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.19), 0 6px 6px rgba(0, 0, 0, 0.23);
            padding:10px;
            background-color: #f2f2f2;
        }

        .form-container h2 {
            font-size: 28px;
            font-weight: bold;
            margin-bottom: 30px;
        }

        .form-group input[type="email"],
        .form-group input[type="password"] {
            width: 70%;
            margin: 0 auto;
        }

        .form-group label {
            font-size: 18px;
            font-weight: bold;
        }
        
    </style>
</head>

<body>
    <h1 style="text-align: center; margin-top: 50px; color: #333; text-shadow: 2px 2px #ddd;">Welcome to Placement Cell</h1>

    <div class="form-container">
        <h2 class="text-center">Sign In</h2>
        <form method="post" action="/create-session">
            <div class="form-group">
                <label for="email">Email address:</label>
                <input type="email" name="email" class="form-control" id="email"  required>
            </div>
            <div class="form-group">
                <label for="pwd">Password:</label>
                <input type="password" name="password" class="form-control" id="pwd"  required>
            </div>
            <button type="submit" class="btn btn-primary">Sign In</button>
        </form>
        <p class="text-center mt-3">Don't have an account? <a href="/sign-up">Create one</a></p>
    </div>
</body>

</html>